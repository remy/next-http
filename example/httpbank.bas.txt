#autoline 10
#program httpbank

BANK 30 ERASE : BANK 31 ERASE

h$="192.168.1.118"
b$="30"

LOAD "httpbank.bas.txt" BANK 30
PRINT "posting"
../httpbank.dot post -b b$ -h h$ -p p$ -l 20
PRINT "done"
PRINT "now getting"
../httpbank.dot get -h h$ -p p$ -b 31 -u / : ; $3E
PRINT "complete, reading"
CLS
BANK 31 COPY 0,6912 TO 5,0: ; load the data into L1
PAUSE 0
