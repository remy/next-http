#autoline 10
#program httbank

h$="next.marbles2.com"
b$="30"

LOAD "httpbank.bas.txt" BANK 30
PRINT "posting"
../httpbank post -b b$ -h h$ -l 20
PRINT "done, now getting"
../httpbank get -h next.marbles2.com -b 31 : ; $3E
PRINT "complete, reading"
CLS
BANK 31 COPY 0,6912 TO 5,0: ; load the data into L1
PAUSE 0
